{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% set title = 'Nowa oferta' %}

{% block body %}
    <div class="col-md-5 mx-auto">
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <button class="btn btn-primary" type="submit">Utwórz ofertę</button>
        {{ form_end(form) }}
    </div>
{% endblock %}